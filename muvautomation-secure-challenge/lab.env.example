# Copien este archivo a lab.env y complétenlo con los datos que dé el docente.
# Luego, en cada terminal (Ubuntu y Kali) antes de correr los scripts:
#   source lab.env
export TARGET_IP=IP_ASIGNADA
export TARGET_URL=http://$TARGET_IP
export LAB_CIDR=CIDR_AUTORIZADO
